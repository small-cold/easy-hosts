var config = {
    // 系统hosts路径，非win、非mac需要自己指定，为空使用系统默认
    // "sysHostsPath": "",
    "commonHostsFileName": "通用",
    // hosts 分组扫描深度，默认为1，即只扫描 EasyHosts/files 下的文件及文件夹内的文件
    "hostsCategoryDeep": 1
    // 远程目录地址
    // ,"remoteHostsFileList":[{name:"file name", url: "127.0.0.1:8080/hosts"}]
};